import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF6A1B9A);
  static const Color primaryLight = Color(0xFF9C4DCC);
  static const Color primaryDark = Color(0xFF38006C);

  static const Color secondary = Color(0xFFF06292); // The accent color
  static const Color background = Color(0xFFF5F5F5); // Or a similar light gray

  // Add other colors from your design system
  static const Color darkText = Color(0xFF212121);
  static const Color lightText = Color(0xFFFFFFFF);
}
